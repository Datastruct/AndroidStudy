//Copy Task on gradle
//from folder to assets
    
task copyConfigFiles(type: Copy) {
    from '../Module/MyFolder/config/'
    into '../Module/assets/'
    include('**/*')
}

tasks.withType(JavaCompile) {
    compileTask -> compileTask.dependsOn copyConfigFiles
}